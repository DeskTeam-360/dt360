import { ServicesTestimonialsCarouselLoader } from "@/components/pages/services/ServicesTestimonialsCarouselLoader";

/** Hanya carousel + data API; markup `<section>` / heading ada di halaman. */
export function ServicesTestimonialsSection() {
  return <ServicesTestimonialsCarouselLoader />;
}
